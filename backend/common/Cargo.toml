[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
once_cell = "1.0"
validator = { version = "0.20", features = ["derive"] }